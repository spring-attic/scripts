[config]
clone = echo 'not cloning submodule'
checkout = git checkout master
deploy = mvn deploy "$@" -DaltDeploymentRepository=repo.spring.io::default::https://repo.spring.io/libs-snapshot-local
install = mvn install "$@"

[cloudfoundry]
clone = echo 'not cloning submodule'
checkout = git checkout master
deploy = mvn deploy "$@" -DaltDeploymentRepository=repo.spring.io::default::https://repo.spring.io/libs-snapshot-local
install = mvn install "$@"

[netflix]
clone = echo 'not cloning submodule'
checkout = git checkout master
deploy = mvn deploy "$@" -DaltDeploymentRepository=repo.spring.io::default::https://repo.spring.io/libs-snapshot-local
install = mvn install "$@"

[bus]
clone = echo 'not cloning submodule'
checkout = git checkout master
deploy = mvn deploy "$@" -DaltDeploymentRepository=repo.spring.io::default::https://repo.spring.io/libs-snapshot-local
install = mvn install "$@"

[cli]
clone = echo 'not cloning submodule'
checkout = git checkout master
deploy = mvn deploy "$@" -DaltDeploymentRepository=repo.spring.io::default::https://repo.spring.io/libs-snapshot-local
install = mvn install "$@" && [ -e ~/.gvm/springboot/dev ] && cp target/*.jar ~/.gvm/springboot/dev/lib

[starters]
clone = echo 'not cloning submodule'
checkout = git checkout master
deploy = mvn deploy "$@" -DaltDeploymentRepository=repo.spring.io::default::https://repo.spring.io/libs-snapshot-local
install = mvn install "$@"

[config-repo]
clone = echo 'not cloning submodule'
checkout = git checkout master

[configserver]
clone = echo 'not cloning submodule'
checkout = git checkout master

[eureka]
clone = echo 'not cloning submodule'
checkout = git checkout master

[customers-stores]
clone = echo 'not cloning submodule'
checkout = git checkout master

[hystrix-dashboard]
clone = echo 'not cloning submodule'
checkout = git checkout master

[turbine]
clone = echo 'not cloning submodule'
checkout = git checkout master

[locks]
clone = echo 'not cloning submodule'
checkout = git checkout master

[]
clone = git clone 'git@github.com:spring-cloud-samples/scripts.git' 'scripts' && git submodule update --init
checkout = git checkout full

